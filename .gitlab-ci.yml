# ✅ Definitieve GitLab Pages configuratie voor KGB SmartShop
image: node:18

stages:
  - deploy

pages:
  stage: deploy
  script:
    - echo "🚀 Start GitLab Pages build"
    - mkdir -p public
    - ls -R public
  artifacts:
    paths:
      - public
  only:
    - main
